{
  "name": "@ubc-genai-toolkit/chunking",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 0",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@ubc-genai-toolkit/core": "file:../core",
    "langchain": "^0.3.21"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
